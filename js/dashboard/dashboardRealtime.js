/* ── dashboardRealtime.js ──────────────────────────────────────────────────
   Interfaz preparada para Realtime. No se activa todavía para mantener esta
   fase enfocada en lectura agregada y migración del Dashboard. */

export const createDashboardRealtime = () => ({
  start() {
    return null;
  },
  stop() {},
});
